#include<bits/stdc++.h>
using namespace std;

int main()
{
    int n, a[n];
    cout<<"Enter Size of Array"<<endl;
    cin>>n;
  cout << "Enter Array element numbers: " << endl;

  for (int i = 0; i < n; ++i) {
    cin >> a[n];
  }
  cout<<"result"<<a[0]<<endl;
}
