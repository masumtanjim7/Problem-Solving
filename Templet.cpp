#include<bits/stdc++.h>
using namespace std;
template <class T>
T maxV (T a, T b){
if (a>b)
    cout << a << endl;
else
    cout << b << endl;
}
int main ()
{
    int a,b;
    cin >> a >> b;
    maxV (a,b);
    char c='g' , d='i';
    maxV (c,d);
    return 0;
}
